<tr>
  <td scope="row"><%= @item.name %></td>
  <td><%= link_to "Show", @item, class: "btn btn-primary" %></td>
  <td><%= link_to "Edit", edit_product_path(@item), class: "btn btn-primary" %></td>
  <td><%= link_to "Destroy", @item, class: "btn btn-primary", method: :delete, data: { confirm: "Are you sure?" } %></td>
</tr>
