<tr>
  <td scope="row"><%= @item.code %></td>
  <td><%= link_to "Show", @item, class: "btn btn-main" %></td>
  <td><%= link_to "Edit", edit_movie_path(@item), class: "btn btn-main" %></td>
  <td><%= link_to "Destroy", @item, class: "btn btn-main", method: :delete, data: { confirm: "Are you sure?" } %></td>
</tr>
